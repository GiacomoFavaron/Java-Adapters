package adapter;

/**
 * Adapter class from CLDC 1.1 Vector to JSE 1.4.2 Collection (interface HCollection) through the ListAdapter class (which implements the HCollection interface). This implementation does not allow null elements.
 */

public class CollectionAdapter extends ListAdapter {
    
}